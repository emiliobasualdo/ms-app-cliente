import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    height: '9%',
    flexDirection: 'row',
    justifyContent: 'space-around'
  }
});
